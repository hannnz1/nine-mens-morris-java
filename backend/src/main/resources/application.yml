server:
  port: ${SERVER_PORT:8080}
  shutdown: graceful
  max-http-request-header-size: 16KB
  error:
    include-message: never
    include-stacktrace: never

spring:
  application:
    name: nine-mens-morris-backend
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/morris}
    username: ${DB_USERNAME:morris}
    password: ${DB_PASSWORD:morris}
    hikari:
      # PostgreSQL session setting, also applied to connections created after startup.
      connection-init-sql: "SET lock_timeout = '${DB_LOCK_TIMEOUT:2s}'"
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  # Initialize a new database explicitly with db/init.sql before starting this app.
  sql:
    init:
      mode: never
  lifecycle:
    timeout-per-shutdown-phase: 20s

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
